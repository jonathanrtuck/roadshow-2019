import React from 'react';
import { Typography } from '@material-ui/core';

/**
 * @constant
 * @function
 * @param {object} props
 * @returns {ReactElement}
 */
const Ngmp = () => <Typography>ngmp…</Typography>;

/**
 * @type {ReactElement}
 */
export default Ngmp;
