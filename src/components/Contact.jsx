import React from 'react';
import { Typography } from '@material-ui/core';

/**
 * @constant
 * @function
 * @param {object} props
 * @returns {ReactElement}
 */
const Contact = () => <Typography>contact…</Typography>;

/**
 * @type {ReactElement}
 */
export default Contact;
