import React from 'react';
import { Typography } from '@material-ui/core';

/**
 * @constant
 * @function
 * @param {object} props
 * @returns {ReactElement}
 */
const Profile = () => <Typography>profile…</Typography>;

/**
 * @type {ReactElement}
 */
export default Profile;
