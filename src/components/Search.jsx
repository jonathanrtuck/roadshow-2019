import React from 'react';
import { Typography } from '@material-ui/core';

/**
 * @constant
 * @function
 * @param {object} props
 * @returns {ReactElement}
 */
const Search = () => <Typography>search…</Typography>;

/**
 * @type {ReactElement}
 */
export default Search;
