import React from 'react';
import { Typography } from '@material-ui/core';

/**
 * @constant
 * @function
 * @param {object} props
 * @returns {ReactElement}
 */
const Chatbot = () => <Typography>chatbot…</Typography>;

/**
 * @type {ReactElement}
 */
export default Chatbot;
