import React from 'react';
import { Typography } from '@material-ui/core';

/**
 * @constant
 * @function
 * @param {object} props
 * @returns {ReactElement}
 */
const Page = () => <Typography>profile…</Typography>;

/**
 * @type {ReactElement}
 */
export default Page;
