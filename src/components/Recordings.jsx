import React from 'react';
import { Typography } from '@material-ui/core';

/**
 * @constant
 * @function
 * @param {object} props
 * @returns {ReactElement}
 */
const Recordings = () => <Typography>recordings…</Typography>;

/**
 * @type {ReactElement}
 */
export default Recordings;
